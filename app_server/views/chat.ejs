<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App</title>
    <link rel="stylesheet" href="../../public/css/style.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css"
        integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js"></script>

</head>

<body onload="send()">
    <div id="header">
        <nav class="navbar navbar-expand-lg navbar-light " style="background-color: #eef1f8;">
            <a class="navbar-brand " href="#" style="font-size: 1.3rem;font-family: 'Noto Sans KR', sans-serif;"> <%= username %>  </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav" style="background-color: #eef1f8;">
                <ul class="navbar-nav">
                    <li class="nav-item ">
                        <a class="nav-link " href="#">Home </a>
                    </li>
                    <li class="nav-item ">
                        <a class="nav-link " href="#">Edit your profile </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link " href="/logout">logout</a>
                    </li>

                </ul>
            </div>
        </nav>
    </div>

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font: 13px Helvetica, Arial;
        }

        form {
            background: #c3c4c7;
            padding: 3px 4px 2px 10px;
            position: fixed;
            bottom: 4px;
            width: 70.3%;
            left: 14.85%;
            height: 55px;
            border-radius: 50px;
        }
        form input {
            border: 0;
            background: #c3c4c7;
            padding: 10px;
            width: 83.5%;
            height: 45px;
            margin-right: 0.5%;
            border:none;
            border-radius: 50px;
            outline: none;
            word-wrap: break-word;
        }

        form button {
            width: 16%;
            height: 50px;
            border: 1.5px solid #fc5c68;
            border-radius: 60px;
            outline: none !important;
        }

        #messages {
            list-style-type: none;
            margin: 0 auto;
            box-shadow: 0 0 5px 1px gray;
            padding: 0;
            padding-bottom: 5px;
            padding-top: 5px;
            height: 85vh;
            width: 70%;
            overflow-y: scroll;
        }

        #messages li {
            margin-top: 2px;
            max-width: 700px;
            border-radius: 5px;
            text-align: left;
            clear: both;
            display: inline-block;
            word-wrap: break-word;
            color: white;

        }

        #messages .lime {
            float: right;
            margin-right: 5px;
            background-color: #fc5c68;
            padding: 10px 20px 10px 30px;
        }

        #messages .lito {
            float: left;
            margin-left: 5px;
            background-color:   #c3c4c7;
            color: black;
            padding: 10px 30px 10px 20px;
        }

        @media only screen and (max-width: 768px) {
            #messages {
                width: 100%;
                height: 72vh;
                box-shadow: 0 0 0 0;
            }

            #messages li {
                max-width: 300px;

            }
            form{
                width: 98%;
                left: 1%;
            }
        }
    </style>
    <script src="/socket.io/socket.io.js"></script>

    <script>
        queryString = window.location.search
        var urlParams = new URLSearchParams(queryString);
        var id = urlParams.get('id');

        function send() {
            var socket = io();
            socket.emit('send', {
                to: id
            });
        }
        $(function () {
            var socket = io();
            $('form').submit(function (e) {
                e.preventDefault(); // prevents page reloading
                if ($('#m').val() != '') {
                    socket.emit('chat message', {
                        to: id,
                        me: $('#hid').val(),
                        message: $('#m').val()
                    });
                    $('#messages').append($("<li class='lime' >").text($('#m').val()), "</li> ");
                    $('#messages').animate({
                        scrollTop: 1000
                    }, '100');
                    $('#m').val('');
                }
                return false;
            });
            socket.on('chat message', function (msg) {
                if (id == msg['id'].trim()) {
                    $('#messages').append($("<li class='lito'>").text(msg['msg']['message']), "</li> ");
                    $('#messages').animate({
                        scrollTop: 1000
                    }, '50');
                }
            });
        });
    </script>

    <ul id="messages">
        <% for(var i = 0; i < messages.length ; i++){ %>
        <% if(messages[i]['fromm'] == userId) {%>
        <li class="lime"> <%= messages[i]['message'] %> </li>
        <% } else{ %>
        <li class="lito"> <%= messages[i]['message'] %> </li>
        <%  }%> 
        <% } %>
    </ul>
    <form action="">
        <input type="hidden" id="hid" value=' <%= userId %>'>
        <input id="m" autocomplete="off" placeholder="Enter your message..." /><button><img width="45" src="../../public/images/send.png" alt=""></button>
    </form>